---
import BaseLayout from '../../layouts/BaseLayout.astro';
import AstroHeader from '../../components/AstroHeader.astro';
import AstroFooter from '../../components/AstroFooter.astro';
import ToolPage from '../../components/ToolPage';
import { getToolById } from '../../config/tools';

export const prerender = true;

// Get the portrait generator tool configuration
const tool = getToolById('ai-portrait-generator');

if (!tool) {
  return Astro.redirect('/404');
}

// SEO data
const seo = {
  path: 'art-prompts/portrait',
  title: tool.seo_title,
  description: tool.seo_description,
  keywords: 'portrait prompts, portrait generator, portrait photography, face art prompts, character portraits'
};
---

<BaseLayout seo={seo}>
  <AstroHeader />

  <ToolPage client:load tool={tool} freeGenerations={5} />

  <AstroFooter />
</BaseLayout>
