# LLM Provider Configuration
# Choose which LLM provider to use: "openai" or "anthropic"
LLM_PROVIDER=openai

# OpenAI Configuration
# Get your API key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# Anthropic Configuration (Alternative)
# Get your API key from: https://console.anthropic.com/
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# YouTube Transcript API Configuration (Optional)
# If using a third-party transcript service, add credentials here
YOUTUBE_TRANSCRIPT_API_KEY=your_transcript_api_key_here
YOUTUBE_TRANSCRIPT_API_URL=https://youtube-transcript-api.example.com

# =============================================================================
# SITE CONFIGURATION
# =============================================================================

# Your production domain (IMPORTANT for OAuth and email links)
# This must match the Site URL configured in Supabase Dashboard
# → Authentication → URL Configuration → Site URL
PUBLIC_SITE_URL=https://randomprompts.org

# =============================================================================
# SUPABASE CONFIGURATION (Authentication & Database)
# =============================================================================

# Your Supabase project URL
# Copy from: Supabase Dashboard → Settings → API → Project URL
PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key (public, safe to expose)
# Copy from: Supabase Dashboard → Settings → API → Project API keys → anon/public
PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Supabase Service Role Key (PRIVATE - never expose to client)
# Copy from: Supabase Dashboard → Settings → API → Project API keys → service_role
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# IMPORTANT: After setting these variables, also configure in Supabase Dashboard:
# 1. Authentication → URL Configuration → Site URL: https://randomprompts.org
# 2. Authentication → URL Configuration → Redirect URLs:
#    - https://randomprompts.org/auth/callback
#    - https://randomprompts.org
#    - http://localhost:4321/auth/callback (for development)
#
# See SUPABASE_EMAIL_AND_AUTH_SETUP.md for complete email and OAuth setup

# =============================================================================
# PAYPAL CONFIGURATION (Payment Processing)
# =============================================================================

# PayPal Client ID (get from PayPal Developer Dashboard)
# For sandbox: developer.paypal.com → Apps & Credentials → Sandbox → Your App
# For production: developer.paypal.com → Apps & Credentials → Live → Your App
PAYPAL_CLIENT_ID=your-paypal-client-id

# PayPal Client Secret (PRIVATE - never expose to client)
PAYPAL_CLIENT_SECRET=your-paypal-client-secret

# PayPal Mode: 'sandbox' for testing, 'production' for live payments
PAYPAL_MODE=sandbox

# Public PayPal Client ID (same as PAYPAL_CLIENT_ID, used in frontend)
PUBLIC_PAYPAL_CLIENT_ID=your-paypal-client-id

# =============================================================================
# ADMIN CONFIGURATION
# =============================================================================

# Admin API Key for manual user upgrades (generate a strong random key)
# Generate with: openssl rand -base64 32
ADMIN_API_KEY=your-secure-random-admin-key

# =============================================================================
# VARIABLE NAME COMPATIBILITY NOTES
# =============================================================================

# If you have these variables from your Supabase setup, here's the mapping:
#
# Your Variable Name              →  What This App Expects
# -----------------------------------------------------------------------
# SUPABASE_URL                    →  PUBLIC_SUPABASE_URL
# NEXT_PUBLIC_SUPABASE_URL        →  PUBLIC_SUPABASE_URL
# SUPABASE_ANON_KEY               →  PUBLIC_SUPABASE_ANON_KEY
# SUPABASE_PUBLISHABLE_KEY        →  PUBLIC_SUPABASE_ANON_KEY
# NEXT_PUBLIC_SUPABASE_ANON_KEY   →  PUBLIC_SUPABASE_ANON_KEY
# SUPABASE_SERVICE_ROLE_KEY       →  SUPABASE_SERVICE_ROLE_KEY (same ✓)
#
# The app will automatically use fallback variable names if the preferred ones
# are not found. However, it's recommended to use PUBLIC_* for Astro apps.

# =============================================================================
# SECURITY CHECKLIST
# =============================================================================

# ⚠️  NEVER commit .env file to git!
# ✅  Make sure .env is in your .gitignore
# 🔒  Keep these PRIVATE (server-side only):
#     - SUPABASE_SERVICE_ROLE_KEY
#     - PAYPAL_CLIENT_SECRET
#     - ADMIN_API_KEY
#     - OPENAI_API_KEY
#     - ANTHROPIC_API_KEY
# ✅  These are PUBLIC (safe to expose):
#     - PUBLIC_SUPABASE_URL
#     - PUBLIC_SUPABASE_ANON_KEY
#     - PUBLIC_PAYPAL_CLIENT_ID
#     - PUBLIC_SITE_URL
